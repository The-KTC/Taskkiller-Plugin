# This is the config-file where you can
# give the tasks which should be closed
# by the plugin. The notation is simple
# just add a comma after a task, except
# for the last one, like for example:

# listedPrograms:
#   - firefox.exe
#   - nextcloud.exe

listedPrograms: